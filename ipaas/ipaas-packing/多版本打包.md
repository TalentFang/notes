


csv
组件,版本,组网

notes: 组件是主组件，可能不包含附带组件。如es和elasticsearch_exporter

config.xml
os, cpu, 组件,版本 找软件路径

问题：主组件，路径包含附带组件


打包目录
os, cpu


hosts模板
所有出现的主组件，附带组件，镜像等都需要版本变量

hosts.py 删除未出现的组




打包流程：
csv, config.xml， 拿到主组件和版本。
打包能拿到镜像，附带组件。正常。
怎样映射到hosts中全部组件版本？**需要有一个地方手动指定映射关系。**

1. 依靠config.yml中的组件依赖关系？ —— images版本不知道，且改动量大
2. 单独配置映射文件
	1. 

