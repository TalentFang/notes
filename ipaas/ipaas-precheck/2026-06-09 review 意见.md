
hosts文件在这里修改ip地址，安装时从这边拷贝
197上打包，打包时替换inventory目录等，jenkins直接出压缩包

197上先自己构建验证一下，同时提供构建文档。jenkins上提供编译脚本，提供第三方依赖包，自动出包

iops不同情况不同计算方式

3/4/5节点，ck没有独立盘,没有bcache，HDD不允许安装
纯SSD  允许合设
不是纯SSD,有没有做BCACHE 允许合设
纯HDD 不允许合设


报告输出时对齐


ssh 端口 user 密码等有限读取server.txt，没有的话读hosts

预检查需要连接remote，应该放在集群中
防火墙设置ip互信即可，不要关闭防火墙

vip检查
* vip绑定前检查是否被占用
* vip Check 绑定之后是否有物理ip
 

md5检查：/data目录下的安装包合md5文件
端口冲突检查：补充组件的端口
gin_port在前置检查中需要检查是否
内核版本检查：大于配置文件中的版本即可安装
最小安装：不同操作系统是否也是这个文件，内容字段是不是也叫这个

独立挂载盘：becache能不能过，主要比较/ 和/data不是mkdir
检查用户：不能有asap用户，有的话直接删了
iops：多一点时间，多压测一下，出一个独立报告

suggestions ：建议需要具备可操作性




=======================
jenkins流水线搭建

compareKernelVersion 内核版本比较
防火墙互信

tocheck 
md5值文件未自动获取
iops报告获取的值不对


fio --name=precheck --filename=/data/.tmp --ioengine=libaio --direct=1 --rw=randrw --bs=4k --numjobs=1 --runtime=30 --time_based --group_reporting --output-format=json --size=1G 


CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o check && scp -o StrictHostKeyChecking=no check root@192.168.11.245:/data/precheck/check