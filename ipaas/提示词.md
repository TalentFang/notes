 帮我创建一个项目级别的memory，存放在当前目录下，以便每次加载时能够加载相关信息：本工程是xdr的企业级ipaas底座开发相关项目，maxs-ops是底座安装包，主要是一些ansible脚本和sh脚本，用于
  控制底座的安装过程；upgrade_shell是底座升级包，其中的develop分支是不同版本的升级包，用tag来标识，而release_seg等分支是patch补丁包；ipaas-packing是打包工具，其放在jenkins中配合jenk
  ins脚本来进行底座安装包的打包；service-monitor-331是负责一些底座的监控，里面主要看python-offline，其它的弃用了，包含device-id上报，日志清理，状态监控等功能；maxs-monitor是和python
  -offline定位类似，属于业务代码部分。如果你想要探索不同工程的代码以便更清晰感知其内容和模块可以自行检索。