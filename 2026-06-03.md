business-install-pre.yml


制作一个docker jdk8的基础镜像->业务重新编->push到repo->k8s

需要知道用到哪些jdk8，覆盖哪些功能，合为一个docker镜像，供业务使用






一： 
https 加密与SSL?

ingress service pod代表什么实体？

ip互信，跨网段防火墙

跨网段的时候，http会明文暴露，得https加密和证书校验


pod

加盐？

service，ingress 在k8s中？

flink中有VPA？


三合一镜像
linux-alpine-asset.tar  
openjdk.tar
openjdk-font.tar

centos7/java8?

openjdk 8u492最新的



单机VPA 多机HPA？ 


jdk基础镜像升级结论：
四合一镜像，删除旧镜像，新增一个镜像
新镜像的java版本是openjdk 8u492



upgrade的整个执行流程？


升级包 不同版本的安装包需要不同分支的升级包


607-xdr-release，的patch，不停patch，然后所有patch打包成一个升级包


605->608跨版本升级：从605开始，叠加每一个版本的升级包。


develop
6.0.5 release    tag
6.0.5 release seg     branch
6.0.4 release patch1.  tag
6.0.4 release packet2.  branch